import subprocess
import json
import urllib.request
import time
import websocket # let's check if websocket is available or write minimal websocket client

# Test with minimal python script using urllib or websocket
print("Testing CDP...")
